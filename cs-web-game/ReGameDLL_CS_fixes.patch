diff --git a/regamedll/CMakeLists.txt b/regamedll/CMakeLists.txt
index 70bf349..39e03c7 100644
--- a/regamedll/CMakeLists.txt
+++ b/regamedll/CMakeLists.txt
@@ -354,7 +354,14 @@ if (CMAKE_BUILD_TYPE MATCHES Unittests)
 	set(LINK_FLAGS "${LINK_FLAGS} -no-pie -Wl,--no-export-dynamic")
 	add_executable(regamedll ${appversion.sh})
 else()
-	add_library(regamedll SHARED ${appversion.sh})
+	if(XASH_EMSCRIPTEN)
+		add_executable(regamedll ${appversion.sh})
+		target_compile_options(regamedll PRIVATE -fPIC -Oz)
+		target_link_options(regamedll PRIVATE -sSIDE_MODULE=1 -Oz)
+		set_target_properties(regamedll PROPERTIES SUFFIX ".wasm")
+	else()
+		add_library(regamedll SHARED ${appversion.sh})
+	endif()
 endif()
 
 if (NOT TARGET appversion)
diff --git a/regamedll/dlls/client.cpp b/regamedll/dlls/client.cpp
index b802a2e..0610937 100644
--- a/regamedll/dlls/client.cpp
+++ b/regamedll/dlls/client.cpp
@@ -15,6 +15,7 @@ int gmsgShowGameTitle = 0;
 int gmsgCurWeapon = 0;
 int gmsgHealth = 0;
 int gmsgDamage = 0;
+int gmsgDamageInfo = 0;
 int gmsgBattery = 0;
 int gmsgTrain = 0;
 int gmsgLogo = 0;
@@ -225,6 +226,7 @@ void LinkUserMessages()
 	gmsgFog           = REG_USER_MSG("Fog", 7);
 	gmsgShowTimer     = REG_USER_MSG("ShowTimer", 0);
 	gmsgHudTextArgs   = REG_USER_MSG("HudTextArgs", -1);
+	gmsgDamageInfo    = REG_USER_MSG("DamageInfo", 4);
 
 #ifdef BUILD_LATEST
 	gmsgAccount       = REG_USER_MSG("Account", 5);
diff --git a/regamedll/dlls/client.h b/regamedll/dlls/client.h
index d873bd2..1e84f7b 100644
--- a/regamedll/dlls/client.h
+++ b/regamedll/dlls/client.h
@@ -224,6 +224,7 @@ extern int gmsgShowGameTitle;
 extern int gmsgCurWeapon;
 extern int gmsgHealth;
 extern int gmsgDamage;
+extern int gmsgDamageInfo;
 extern int gmsgBattery;
 extern int gmsgTrain;
 extern int gmsgLogo;
diff --git a/regamedll/dlls/player.cpp b/regamedll/dlls/player.cpp
index 1d04cc9..940f08f 100644
--- a/regamedll/dlls/player.cpp
+++ b/regamedll/dlls/player.cpp
@@ -28,6 +28,22 @@
 
 #include "precompiled.h"
 
+static bool IsValidRealClient(edict_t* pEdict)
+{
+	if (!pEdict || pEdict->free)
+		return false;
+
+	const int index = ENTINDEX(pEdict);
+
+	if (index < 1 || index > gpGlobals->maxClients)
+		return false;
+
+	if (!(pEdict->v.flags & FL_CLIENT))
+		return false;
+
+	return true;
+}
+
 BOOL gInitHUD = TRUE;
 
 TYPEDESCRIPTION CBasePlayer::m_playerSaveData[] =
@@ -246,7 +262,15 @@ bool EXT_FUNC CBasePlayer::__API_HOOK(SetClientUserInfoName)(char *infobuffer, c
 
 void EXT_FUNC CBasePlayer::SetClientUserInfoModel_api(char *infobuffer, char *szNewModel)
 {
-	SET_CLIENT_KEY_VALUE(entindex(), infobuffer, "model", szNewModel);
+	if (IsValidRealClient(edict()))
+	{
+		SET_CLIENT_KEY_VALUE(entindex(), infobuffer, "model", szNewModel);
+	}
+	else
+	{
+		ALERT(at_console, "[PHYSKEY BLOCKED] SetClientUserInfoModel_api -> index=%d classname=%s flags=%d\n",
+			ENTINDEX(edict()), STRING(pev->classname), pev->flags);
+	}
 }
 
 void CBasePlayer::SetClientUserInfoModel(char *infobuffer, char *szNewModel)
@@ -1303,10 +1327,15 @@ BOOL EXT_FUNC CBasePlayer::__API_HOOK(TakeDamage)(entvars_t *pevInflictor, entva
 
 	LogAttack(pAttack, this, bTeamAttack, flDamage, armorHit, pev->health - flDamage, pev->armorvalue, GetKillerWeaponName(pevInflictor, pevAttacker));
 
+	const float healthBefore = pev->health > 0.0f ? pev->health : 0.0f;
+
 	// this cast to INT is critical!!! If a player ends up with 0.5 health, the engine will get that
 	// as an int (zero) and think the player is dead! (this will incite a clientside screentilt, etc)
 	bTookDamage = CBaseMonster::TakeDamage(pevInflictor, pevAttacker, int(flDamage), bitsDamageType);
 
+	const float healthAfter = pev->health > 0.0f ? pev->health : 0.0f;
+	const int damageDealt = static_cast<int>((healthBefore - healthAfter) + 0.5f);
+
 	if (bTookDamage)
 	{
 		if (TheBots)
@@ -1326,6 +1355,31 @@ BOOL EXT_FUNC CBasePlayer::__API_HOOK(TakeDamage)(entvars_t *pevInflictor, entva
 #ifdef REGAMEDLL_API
 		CSPlayer()->RecordDamage(pAttack, flDamage);
 #endif
+
+		if (damageDealt > 0 && (bitsDamageType & DMG_BULLET) && pevAttacker && pevAttacker != pev)
+		{
+			CBaseEntity* attackerEntity = CBaseEntity::Instance(pevAttacker);
+			if (attackerEntity && attackerEntity->IsPlayer())
+			{
+				CBasePlayer* attackerPlayer = static_cast<CBasePlayer*>(attackerEntity);
+				edict_t* attackerEdict = attackerPlayer->edict();
+				const int attackerIndex = ENTINDEX(attackerEdict);
+
+				if (attackerEdict && !attackerEdict->free && attackerIndex >= 1 && attackerIndex <= gpGlobals->maxClients)
+				{
+					int hitGroup = m_LastHitGroup;
+					int flags = 0;
+					if (pev->health <= 0.0f)
+						flags |= 1;
+
+					MESSAGE_BEGIN(MSG_ONE_UNRELIABLE, gmsgDamageInfo, nullptr, attackerPlayer->pev);
+					WRITE_SHORT(damageDealt);
+					WRITE_BYTE(hitGroup);
+					WRITE_BYTE(flags);
+					MESSAGE_END();
+				}
+			}
+		}
 	}
 
 	{
@@ -5745,8 +5799,13 @@ void EXT_FUNC CBasePlayer::__API_HOOK(Spawn)()
 	pev->friction = 1;
 	pev->gravity = 1;
 
-	SET_PHYSICS_KEY_VALUE(edict(), "slj", "0");
-	SET_PHYSICS_KEY_VALUE(edict(), "hl", "1");
+#ifndef CLIENT_WEAPONS
+	if (IsValidRealClient(edict()))
+	{
+		SET_PHYSICS_KEY_VALUE(edict(), "slj", "0");
+		SET_PHYSICS_KEY_VALUE(edict(), "hl", "1");
+	}
+#endif
 	m_hintMessageQueue.Reset();
 
 	m_flVelocityModifier = 1;
diff --git a/regamedll/dlls/sound.cpp b/regamedll/dlls/sound.cpp
index 25e930e..8a239b6 100644
--- a/regamedll/dlls/sound.cpp
+++ b/regamedll/dlls/sound.cpp
@@ -1,5 +1,21 @@
 #include "precompiled.h"
 
+static bool IsValidRealClient(edict_t* pEdict)
+{
+	if (!pEdict || pEdict->free)
+		return false;
+
+	const int index = ENTINDEX(pEdict);
+
+	if (index < 1 || index > gpGlobals->maxClients)
+		return false;
+
+	if (!(pEdict->v.flags & FL_CLIENT))
+		return false;
+
+	return true;
+}
+
 // presets for runtime pitch and vol modulation of ambient sounds
 dynpitchvol_t rgdpvpreset[MAX_SENTENCE_DPV_RESET] =
 {
@@ -918,6 +934,13 @@ void CEnvSound::Think()
 		goto env_sound_Think_slow;
 	}
 
+	if (!IsValidRealClient(pentPlayer))
+	{
+		ALERT(at_console, "[ENV_SOUND] Invalid client: index=%d classname=%s flags=%d\n",
+			ENTINDEX(pentPlayer), STRING(pentPlayer->v.classname), pentPlayer->v.flags);
+		goto env_sound_Think_slow;
+	}
+
 	{
 		CBasePlayer *pPlayer = GetClassPtr<CCSPlayer>((CBasePlayer *)VARS(pentPlayer));
 		float flRange;
@@ -974,9 +997,14 @@ void CEnvSound::Think()
 				// only!
 
 				//CLIENT_COMMAND(pentPlayer, "room_type %f", m_flRoomtype);
+#ifndef __EMSCRIPTEN__
+				if (!IsValidRealClient(pentPlayer))
+					goto env_sound_Think_slow;
+
 				MESSAGE_BEGIN(MSG_ONE, SVC_ROOMTYPE, nullptr, pentPlayer);	// use the magic #1 for "one client"
 					WRITE_SHORT((short)m_flRoomtype);						// sequence number
 				MESSAGE_END();
+#endif
 
 				// crank up nextthink rate for new active sound entity
 				// by falling through to think_fast...
diff --git a/regamedll/game_shared/counter.h b/regamedll/game_shared/counter.h
index 84f03ce..1fb9a33 100644
--- a/regamedll/game_shared/counter.h
+++ b/regamedll/game_shared/counter.h
@@ -1,4 +1,4 @@
-﻿/*
+/*
 *
 *   This program is free software; you can redistribute it and/or modify it
 *   under the terms of the GNU General Public License as published by the
@@ -38,7 +38,7 @@
 	#include <sys/types.h>
 	#include <fcntl.h>
 	#include <unistd.h>
-	#ifdef __APPLE__
+	#if defined(__APPLE__) || defined(__EMSCRIPTEN__)
 		#include <limits.h>
 	#else
 		#include <linux/limits.h>
diff --git a/regamedll/public/tier0/platform.h b/regamedll/public/tier0/platform.h
index eaf294f..0a9f449 100644
--- a/regamedll/public/tier0/platform.h
+++ b/regamedll/public/tier0/platform.h
@@ -72,10 +72,11 @@
 #define DLL_CLASS_EXPORT __declspec(dllexport)
 #define DLL_CLASS_IMPORT __declspec(dllimport)
 
+// Can't use extern "C" when DLL exporting a global
 // Can't use extern "C" when DLL exporting a global
 #define DLL_GLOBAL_EXPORT extern __declspec(dllexport)
 #define DLL_GLOBAL_IMPORT extern __declspec(dllimport)
-#elif defined __linux__ || defined __APPLE__
+#elif defined __linux__ || defined __APPLE__ || defined __EMSCRIPTEN__
 
 // Used for dll exporting and importing
 #define DLL_EXPORT extern "C"
